{
  "name": "dfkapi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "npm run clean --workspaces",
    "postinstall": "npm run compile",
    "compile": "tsc -b tsconfig.build.json",
    "test": "npm test --workspaces",
    "start:web": "node ./packages/web/lib/server.js"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "typescript": "^4.7.2"
  }
}
